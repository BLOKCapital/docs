# Dependencies
/node_modules

# Next.js
/.next
/out

# Production
/build

# Generated
/src/lib/search-index.json
tsconfig.tsbuildinfo
next-env.d.ts
# Agent-facing Markdown twins + freshness map (rebuilt by predev/prebuild).
# The route manifest (src/lib/generated/markdown-routes.json) IS committed —
# typecheck and the edge middleware bundle import it without a prior build.
/public/en/
/public/es/
/public/fr/
/public/last-updated.json

# Misc
.DS_Store
.env*.local
.vercel
npm-debug.log*
